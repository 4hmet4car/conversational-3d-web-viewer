export const cameraParameters = {
    enablePan: true,
    enableZoom: true,
    enableRotate: true,
    enableDamping: true,
}

export const rendererParameters = {
    clearColor: '#000000',
}

export const environmentParameters = {
    pointLight: {
        color: '#ffffff',
        intensity: 30,
        positionX: 0,
        positionY: 5,
        positionZ: 0,
    }
}